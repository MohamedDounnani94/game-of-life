# game-of-life

> Game of Life

## Build Setup

``` bash
# install dependencies
$ yarn install

# serve with hot reload at localhost:3000
$ yarn dev

# test
$ yarn test
```
Game of life con `vue`, tests con `ava`